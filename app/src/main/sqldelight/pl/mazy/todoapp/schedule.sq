CREATE TABLE IF NOT EXISTS schedule(
    Name TEXT NOT NULL,
    Description TEXT,
    TimeStart TEXT,
    TimeEnd TEXT,
    Date TEXT,
    Type TEXT,
    Color TEXT NOT NULL
);

selFromDay:
SELECT * FROM schedule WHERE Date = ?;